'use strict'

const balanceCero = require('./balance-cero-peso')
const balanceDosPesos = require('./balance-dos-pesos')
const balanceUnPeso = require('./balance-un-peso')
const crearMapa = require('./crear-mapa')
const diferencia = require('./diferencia')
const numerosReales = require('./numeros-reales')
const ordenaArreglo = require('./ordena-arreglo')

module.exports = {
  balanceCero,
  balanceDosPesos,
  balanceUnPeso,
  crearMapa,
  diferencia,
  numerosReales,
  ordenaArreglo
}
